import { ArreImagesPipe } from './arre-images.pipe';

describe('ArreImagesPipe', () => {
  it('create an instance', () => {
    const pipe = new ArreImagesPipe();
    expect(pipe).toBeTruthy();
  });
});
